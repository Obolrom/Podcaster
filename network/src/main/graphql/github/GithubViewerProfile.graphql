query GetGithubViewerProfile {
    viewer {
        id
        login
        avatarUrl
        email
        status {
            message
            emoji
        }
        followers {
            totalCount
        }
        following {
            totalCount
        }
        repositories {
            totalCount
        }
    }
}