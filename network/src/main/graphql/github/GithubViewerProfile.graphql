query GetGithubViewerProfile {
    viewer {
        id
        login
        avatarUrl
        email
        followers {
            totalCount
        }
        repositories {
            totalCount
        }
    }
}